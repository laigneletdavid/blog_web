{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier les données de mon compte</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary mb-5 mt-3">{{ button_label|default('Sauvegarder') }}</button>
        {{ form_end(form) }}

        </div>
{% endblock %}
