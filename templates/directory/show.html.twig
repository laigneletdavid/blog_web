{% extends 'base.html.twig' %}

{% block seo %}
    <title>{{ seo.title|default(entry.company ?? entry.displayName) }} — {{ site.name|default('') }}</title>
    {% if seo.description|default('') is not empty %}
    <meta name="description" content="{{ seo.description }}">
    {% endif %}
    {% if entry.noIndex %}<meta name="robots" content="noindex, nofollow">{% endif %}
{% endblock %}

{% block body %}
<div class="container py-5">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_home') }}">Accueil</a></li>
            <li class="breadcrumb-item"><a href="{{ path('app_directory') }}">Annuaire</a></li>
            <li class="breadcrumb-item active" aria-current="page">{{ entry.company ?? entry.displayName }}</li>
        </ol>
    </nav>

    <div class="directory-detail">
        <div class="row g-5">
            {# ====== COLONNE PRINCIPALE ====== #}
            <div class="col-lg-8">
                {# Header : photo + identite #}
                <div class="directory-detail__header">
                    {% if entry.photo %}
                    <div class="directory-detail__hero-img mb-4">
                        <img src="{{ asset('documents/medias/' ~ entry.photo) }}" alt="{{ entry.company ?? entry.displayName }}" loading="lazy">
                    </div>
                    {% endif %}
                    <div class="directory-detail__identity">
                        {% if entry.category %}
                        <span class="directory-detail__badge">{{ entry.category.name }}</span>
                        {% endif %}
                        <h1 class="directory-detail__name">{{ entry.company ?? entry.displayName }}</h1>
                        <p class="directory-detail__person">
                            {{ bi('person-fill', 'me-1') }}{{ entry.displayName }}{% if entry.jobTitle %} — {{ entry.jobTitle }}{% endif %}
                        </p>
                        {% if entry.city %}
                        <p class="directory-detail__city">{{ bi('geo-alt-fill', 'me-1') }}{{ entry.city }}</p>
                        {% endif %}
                    </div>
                </div>

                {# Bio / A propos #}
                {% if entry.bio %}
                <div class="directory-detail__bio mt-4">
                    <h2 class="directory-detail__section-title">A propos</h2>
                    <div class="directory-detail__bio-content">
                        {{ entry.bio|raw }}
                    </div>
                </div>
                {% endif %}

                {# Tags rattaches, groupes par famille #}
                {% if entry.tags|length > 0 %}
                <div class="directory-detail__tags mt-4">
                    {# Tags sans famille #}
                    {% set ungrouped = entry.tags|filter(t => t.tagGroup is null) %}
                    {% if ungrouped|length > 0 %}
                        <div class="directory-tag-filters__group mb-2">
                            {% for tag in ungrouped %}
                                <a href="{{ path('app_directory', {tags: [tag.slug]}) }}"
                                   class="directory-tag-filters__pill">{{ tag.name }}</a>
                            {% endfor %}
                        </div>
                    {% endif %}
                    {# Tags groupes par famille #}
                    {% set groups = {} %}
                    {% for tag in entry.tags %}
                        {% if tag.tagGroup %}
                            {% set gid = tag.tagGroup.id %}
                            {% set existing = groups[gid]|default({group: tag.tagGroup, tags: []}) %}
                            {% set groups = groups|merge({(gid): {group: tag.tagGroup, tags: existing.tags|merge([tag])}}) %}
                        {% endif %}
                    {% endfor %}
                    {% for entry_grp in groups %}
                        <div class="directory-tag-filters__group mb-2">
                            <span class="directory-tag-filters__label" style="--tg-color: {{ entry_grp.group.color }};">
                                {{ entry_grp.group.name }}
                            </span>
                            {% for tag in entry_grp.tags %}
                                <a href="{{ path('app_directory', {tags: [tag.slug]}) }}"
                                   class="directory-tag-filters__pill"
                                   style="--tg-color: {{ entry_grp.group.color }};">{{ tag.name }}</a>
                            {% endfor %}
                        </div>
                    {% endfor %}
                </div>
                {% endif %}

                {# Bouton edition si proprietaire #}
                {% if app.user and entry.user and app.user.id == entry.user.id %}
                <div class="mt-4">
                    <a href="{{ path('app_directory_my_entry') }}" class="btn btn-primary btn-sm">{{ bi('pencil', 'me-1') }}Modifier ma fiche</a>
                </div>
                {% endif %}
            </div>

            {# ====== SIDEBAR ====== #}
            <div class="col-lg-4">
                <aside class="directory-sidebar">
                    {# Carte contact #}
                    {% if entry.email or entry.phone %}
                    <div class="directory-sidebar__card">
                        <h3 class="directory-sidebar__title">{{ bi('telephone-fill', 'me-2') }}Contact</h3>
                        <ul class="directory-sidebar__list">
                            {% if entry.email %}
                            <li>
                                {{ bi('envelope') }}
                                <a href="mailto:{{ entry.email }}">{{ entry.email }}</a>
                            </li>
                            {% endif %}
                            {% if entry.phone %}
                            <li>
                                {{ bi('telephone') }}
                                <a href="tel:{{ entry.phone }}">{{ entry.phone }}</a>
                            </li>
                            {% endif %}
                            {% if entry.city %}
                            <li>
                                {{ bi('geo-alt') }}
                                <span>{{ entry.city }}</span>
                            </li>
                            {% endif %}
                        </ul>
                    </div>
                    {% endif %}

                    {# Carte reseaux sociaux #}
                    {% if entry.website or entry.linkedin or entry.facebook or entry.instagram %}
                    <div class="directory-sidebar__card">
                        <h3 class="directory-sidebar__title">{{ bi('link-45deg', 'me-2') }}Liens</h3>
                        <div class="directory-sidebar__links">
                            {% if entry.website %}
                            <a href="{{ entry.website }}" target="_blank" rel="noopener noreferrer" class="directory-sidebar__link">
                                {{ bi('globe') }}<span>Site web</span>{{ bi('box-arrow-up-right', 'ms-auto') }}
                            </a>
                            {% endif %}
                            {% if entry.linkedin %}
                            <a href="{{ entry.linkedin }}" target="_blank" rel="noopener noreferrer" class="directory-sidebar__link">
                                {{ bi('linkedin') }}<span>LinkedIn</span>{{ bi('box-arrow-up-right', 'ms-auto') }}
                            </a>
                            {% endif %}
                            {% if entry.facebook %}
                            <a href="{{ entry.facebook }}" target="_blank" rel="noopener noreferrer" class="directory-sidebar__link">
                                {{ bi('facebook') }}<span>Facebook</span>{{ bi('box-arrow-up-right', 'ms-auto') }}
                            </a>
                            {% endif %}
                            {% if entry.instagram %}
                            <a href="{{ entry.instagram }}" target="_blank" rel="noopener noreferrer" class="directory-sidebar__link">
                                {{ bi('instagram') }}<span>Instagram</span>{{ bi('box-arrow-up-right', 'ms-auto') }}
                            </a>
                            {% endif %}
                        </div>
                    </div>
                    {% endif %}

                    {# Retour #}
                    <div class="directory-sidebar__back mt-3">
                        <a href="{{ path('app_directory') }}" class="text-muted">{{ bi('arrow-left', 'me-1') }}Retour a l'annuaire</a>
                    </div>
                </aside>
            </div>
        </div>
    </div>
</div>
{% endblock %}
