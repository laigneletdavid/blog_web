framework:
    rate_limiter:
        contact_limiter:
            policy: 'sliding_window'
            limit: 3
            interval: '1 minute'
        subscribe_limiter:
            policy: 'sliding_window'
            limit: 5
            interval: '10 minutes'
